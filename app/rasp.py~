

import RPi.GPIO as GPIO

GPIO.setmode(GPIO.BOARD)

focos = {
    40:{'name':'GPIO 40', 'state':GPIO.LOW},
    38:{'name':'GPIO 38', 'state':GPIO.LOW},
    37:{'name':'GPIO 37', 'state':GPIO.LOW}
}

for foco in focos:
    GPIO.setup(foco, GPIO.OUT)
    GPIO.output(foco, GPIO.LOW)
